<%= form_for(@match) do |f| %>
  <% if @match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% @match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :play_date %><br>
    <%= f.date_select :play_date %>
  </div>
  <div class="field">
    <%= f.collection_select(:team_1_id, Team.all, :id, :name, :class => "btn") %>
  </div>
  <div class="field">
    <%= f.collection_select(:team_2_id, Team.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :season_of_competition_id %><br>
    <%= f.text_field :season_of_competition_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
