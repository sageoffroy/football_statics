<%= form_for(@season_of_competition) do |f| %>
  <% if @season_of_competition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season_of_competition.errors.count, "error") %> prohibited this season_of_competition from being saved:</h2>

      <ul>
      <% @season_of_competition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :season_id %><br>
    <%= f.text_field :season_id %>
  </div>
  <div class="field">
    <%= f.label :competition_id %><br>
    <%= f.text_field :competition_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
